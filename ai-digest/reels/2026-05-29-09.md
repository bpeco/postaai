El JSON que llegó por stdin está vacío (`[]`) — no hay ningún item de noticias para laburar.

No puedo generar las 10 ideas de Reels porque las reglas son claras: **no inventar datos que no están en el JSON**. Sin items (title, url, summary, source, engagement) no tengo de dónde sacar las noticias, los claims ni las fuentes.

Esto normalmente pasa cuando una fase anterior del pipeline falló o devolvió vacío. Para destrabarlo:

- **Si estás testeando la fase aislada**, pasá el archivo rankeado de verdad:
  ```bash
  cat /tmp/digest-top-STAMP.json | claude -p "$(cat prompts/reels.md)"
  ```
- **Si corriste el pipeline completo** y el top salió vacío, revisá las fases 1-3:
  ```bash
  ./scripts/run-digest.sh --dry-run        # corre fetch + extract + rank, ~5s
  ls -t /tmp/digest-top-*.json | head -1 | xargs cat   # ¿tiene items?
  ```
  Si `digest-top` está vacío, el problema está aguas arriba (fetch sin respuesta de las fuentes, o el extract/rank filtrando todo).

Pasame el JSON con items y te tiro las 10 ideas al toque. 🎬
