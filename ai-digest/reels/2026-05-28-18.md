Che, me llegó un JSON **vacío** (`[]`) por stdin — cero noticias para laburar.

No puedo generar las 10 ideas de Reels sin items: una de las reglas duras es **no inventar datos que no estén en el JSON**, y acá no hay ni un título, ni una URL, ni un summary. Si me pongo a fabricar noticias AI de la nada, te entrego humo, que es justo lo contrario de lo que pide el brief.

Para que salgan los 10 Reels necesito que el array traiga los ~15 items reales de las últimas 48h. Probablemente lo que pasó es esto:

- La fase de fetch/extract/rank no corrió o devolvió vacío, y se pipeó un `[]`.
- O el comando de prueba mandó el array vacío a propósito.

**Para destrabarlo**, corré el pipeline y pasame el top real. Por ejemplo:

```bash
cat /tmp/digest-top-*.json | claude -p "$(cat prompts/reels.md)"
```

o, si querés generar el JSON fresco primero:

```bash
./scripts/run-digest.sh --dry-run    # corre fases 1-3 y deja /tmp/digest-top-STAMP.json
```

Pasame ese JSON con items adentro y te tiro los 10 Reels al toque, con el balance pedido (4 "te cambia el día", 2 polémico, 2 explainer, 1 futuro, 1 cuidado con). ¿Lo corrés y me lo mandás?
