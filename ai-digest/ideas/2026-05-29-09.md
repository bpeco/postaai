El JSON que recibí por stdin está vacío (`[]`) — cero items de noticias.

No puedo generar las 8 ideas: la regla dura es que cada una tiene que salir de un item distinto del JSON, y "no inventes datos". Sin noticias adentro no hay de dónde sacar hooks, fuentes ni claims reales, y ponerme a inventar releases de Anthropic/OpenAI sería exactamente lo que el prompt prohíbe.

Probable causa (si esto viene del pipeline):

- La fase `[3] rank-items.py` devolvió un top vacío → revisá `/tmp/digest-top-STAMP.json`.
- O más arriba: `fetch-sources.sh` no trajo nada (¿red caída? ¿fuentes rotas?) o `extract-items.py` no parseó items → mirá `/tmp/digest-items-STAMP.json` y `/tmp/digest-raw-STAMP.json`.

Pasame el array con los ~15 items y te tiro las 8 ideas al toque. O si querés, decime y reviso los `/tmp/digest-*.json` del último run para ver en qué fase se cortó.
