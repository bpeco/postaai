No recibí items para trabajar: el JSON que llegó por stdin es un array vacío (`[]`).

Sin noticias no puedo generar las 8 ideas — y armarlas igual significaría inventar releases, números y fuentes, justo lo que las reglas prohíben ("No inventes datos", `Sources` reales, control de `risk_flags`).

Probablemente falló una fase anterior del pipeline. Para chequear rápido:

```bash
# ¿el ranking quedó vacío?
ls -t /tmp/digest-top-*.json | head -1 | xargs cat | jq 'length'

# ¿y la extracción?
ls -t /tmp/digest-items-*.json | head -1 | xargs cat | jq 'length'

# último log del run
ls -t /tmp/ai-digest-*.log | head -1 | xargs cat
```

Si `digest-items` también da 0, el problema está en el fetch (fases 1-2); si `digest-items` tiene data pero `digest-top` está vacío, mirá `rank-items.py`.

Pasame el JSON con items y te devuelvo las 8 ideas (3 noticia rápida + 3 explainer + 2 hot take) al toque.
